node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Logs
*.log

# Raw training datasets — large source CSVs (280MB+, one file exceeds
# GitHub's 100MB limit). Not reproducible from code; see ml/data/raw's
# origin in memory/project_ml_pipeline_status.md if this needs restoring.
ml/data/raw/

# Third-party real-world datasets downloaded for evaluation — licensing
# terms unclear/citation-only for redistribution, kept local only.
# See ml/data/external/README.md (tracked) for sources + how to re-fetch.
ml/data/external/*.csv
ml/data/external/mendeley_sms_phishing/
ml/data/external/smishtank_dataset.csv

# Trained model bundles live outside the repo on D:/ai-scam-detection-data/
